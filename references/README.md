# Instrument references

Manuals, calibration certificates, SDKs, drivers, and runtime vendor libraries are
separated by role here. `reference_registry.yaml` gives their canonical locations.
Runtime code may depend on an SDK library; it does not depend on a manual or
completed campaign directory.
