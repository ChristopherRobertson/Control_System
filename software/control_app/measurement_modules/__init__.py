"""Independent measurement packages discovered via <experiment_id>/registration.py.

Do not add feature imports here. Each package owns its experiment engine, widgets,
tests and procedure; the measurement_host API is the only integration boundary.
"""
