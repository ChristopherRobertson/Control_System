Repo-local PicoSDK runtime copied from:

`C:\Program Files\Pico Technology\PicoScope 7 T&M Stable`

Files copied:

- `ps5000a.dll`
- `picoipp.dll`
- `picocv.dll`

These are loaded before system PicoSDK paths so the control workflow uses the
same runtime family as the PicoScope 7 UI verified with the PicoScope 5244D.

Refreshed after PicoScope 7 update on 2026-06-16:
