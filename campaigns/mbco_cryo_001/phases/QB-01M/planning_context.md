# QB-01M planning context

This file preserves the phase-specific row or rows from the pre-migration
cross-phase planning matrices. The canonical phase plan is `plan.md`; this
context is retained so no planning detail is lost during the phase-primary
restructure.

## Source: characterization measurement matrix

| Field | Preserved value |
| --- | --- |
| `phase_id` | QB-01M |
| `purpose` | Optional cryogenic MbCO probe and acquisition optimization |
| `dependencies` | HRP closeout restoration handoff plus cryostat inputs |
| `emission_allowed` | qualified only after separate authorization |
| `minimum_repetitions` | prospectively precision-selected |
| `mandatory_raw_products` | pulse timing detector Pico HF2LI cryostat and three-mode surrogate evidence |
| `mandatory_results` | qualified direct and lock-in envelopes duty margin and recovery limits |
| `closeout_gate` | all MbCO-specific gates pass without altering core promotion |
| `current_status` | PLANNED OPTIONAL NOT EXECUTED |
