# SV-01 planning context

This file preserves the phase-specific row or rows from the pre-migration
cross-phase planning matrices. The canonical phase plan is `plan.md`; this
context is retained so no planning detail is lost during the phase-primary
restructure.

## Source: characterization measurement matrix

| Field | Preserved value |
| --- | --- |
| `phase_id` | SV-01 |
| `purpose` | Acquire one specimen-matched FTIR reference set each for polystyrene and Mylar |
| `dependencies` | system_recalibration_001:SP-01 CH-00 |
| `emission_allowed` | no |
| `minimum_repetitions` | 1 accepted reference set per material |
| `mandatory_raw_products` | native FTIR exports metadata and normalized CSV |
| `mandatory_results` | feature authority matched geometry preprocessing and uncertainty |
| `closeout_gate` | calibration and validation records sealed and partitioned |
| `current_status` | PLANNED |
