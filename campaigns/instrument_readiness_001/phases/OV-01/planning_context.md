# OV-01 planning context

This file preserves the phase-specific row or rows from the pre-migration
cross-phase planning matrices. The canonical phase plan is `plan.md`; this
context is retained so no planning detail is lost during the phase-primary
restructure.

## Source: characterization measurement matrix

| Field | Preserved value |
| --- | --- |
| `phase_id` | OV-01 |
| `purpose` | Measure overlap only for shared fixed-iris OPO-540 with HRP and MbCO probe geometries |
| `dependencies` | OG-01 |
| `emission_allowed` | no until separately approved |
| `minimum_repetitions` | 3 placements plus 1 downstream realignment per retained pair with no iris adjustment |
| `mandatory_raw_products` | native overlap scans images blocked controls and iris configuration readbacks |
| `mandatory_results` | overlap fraction area or volume placement repeatability aperture-margin validity and uncertainty |
| `closeout_gate` | two-pair alignment procedure accepted without fitting the iris to a desired response |
| `current_status` | PLANNED |
